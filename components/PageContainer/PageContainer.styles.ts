import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  wrapper: {
    background: 'url(https://looparadise.ru/static/media/main12.396b6d25.png)',
    backgroundSize: 'cover',
    backgroundAttachment: 'fixed',
    minHeight: '100vh',
  },
}));
